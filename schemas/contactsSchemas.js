// import Joi from "joi";
const Joi = require("joi")

const createContactSchema = Joi.object({

})

 const updateContactSchema = Joi.object({

})


module.exports = {
    createContactSchema,
    updateContactSchema
}